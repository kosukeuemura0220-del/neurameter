{
  "name": "@neurameter/worker",
  "version": "0.1.0",
  "private": true,
  "description": "NeuraMeter Ingestion API — Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "wrangler": "^4.0.0"
  }
}
